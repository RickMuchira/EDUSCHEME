# Core FastAPI dependencies
fastapi
uvicorn[standard]
python-multipart

# Database
sqlalchemy
pydantic
pydantic[email]

# Development and utilities
python-dotenv
typing-extensions

# Optional: For production deployment
gunicorn

# Optional: For enhanced logging
structlog

# Optional: For CORS (already included in FastAPI)
# fastapi-cors

# Optional: For authentication (if needed later)
# python-jose[cryptography]
# passlib[bcrypt]
# python-multipart

groq==0.4.1
python-docx==0.8.11
reportlab==4.0.7
jinja2==3.1.2
